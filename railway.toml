# Railway 배포 설정 (Nixpacks)
# HTTP MCP Server for KERIS Education API

[build]
  builder = "NIXPACKS"

[deploy]
  numReplicas = 1
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
  startCommand = "node dist/index-http.js"

[service]
  # Railway가 자동으로 PORT 환경 변수 할당
