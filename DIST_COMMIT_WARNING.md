# ⚠️ dist/ 폴더 커밋 관련 중요 안내

## 📌 현재 상태

이 리포지토리는 **빌드된 파일(`dist/`)을 Git에 포함**하고 있습니다.

### 이유
- Smithery 배포 플랫폼의 빌드 검증 요구사항 충족
- 사용자가 빌드 도구 없이 바로 실행 가능

---

## ⚠️ **중요: 코드 수정 시 필수 작업 흐름**

소스 코드를 수정할 때마다 **반드시** 다음 순서를 따라야 합니다:

### 1️⃣ 소스 코드 수정
```bash
# 예: src/core/provider-loader.ts 수정
vim src/core/provider-loader.ts
```

### 2️⃣ 빌드 실행 (필수!)
```bash
npm run build
```

### 3️⃣ 소스와 빌드 파일 함께 커밋
```bash
git add src/ dist/
git commit -m "feat: Add new feature"
git push
```

---

## ❌ 하지 말아야 할 것

```bash
# ❌ 잘못된 예: 빌드 없이 소스만 커밋
git add src/
git commit -m "Update code"
git push
# → dist/가 업데이트되지 않아 소스와 빌드 파일이 불일치!
```

---

## 🔄 올바른 작업 흐름 예시

### 예시 1: 새 기능 추가

```bash
# 1. 브랜치 생성
git checkout -b feature/new-provider

# 2. 코드 작성
vim src/core/new-feature.ts

# 3. 빌드
npm run build

# 4. 테스트
npm start

# 5. 커밋 (소스 + 빌드)
git add src/ dist/
git commit -m "feat: Add new provider support"

# 6. 푸시
git push origin feature/new-provider
```

### 예시 2: 버그 수정

```bash
# 1. 버그 수정
vim src/lib/toon-parser.ts

# 2. 빌드 (꼭 필요!)
npm run build

# 3. 커밋
git add src/lib/toon-parser.ts dist/lib/toon-parser.js dist/lib/toon-parser.d.ts
git commit -m "fix: Resolve TOON parsing issue"
git push
```

---

## 📊 동기화 확인 방법

빌드 파일과 소스가 동기화되어 있는지 확인:

```bash
# 1. 현재 상태 확인
git status

# 2. 빌드 실행
npm run build

# 3. 변경사항 확인
git status

# 변경사항이 없으면 → 동기화됨 ✅
# 변경사항이 있으면 → 커밋 필요 ❌
```

---

## 🛠️ 자동화 스크립트 (권장)

빌드를 잊지 않도록 pre-commit hook 설정:

```bash
# .git/hooks/pre-commit 생성
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/bash
echo "Running build before commit..."
npm run build

# dist/ 폴더의 변경사항 자동 추가
git add dist/

echo "✅ Build completed and dist/ added"
EOF

chmod +x .git/hooks/pre-commit
```

이제 `git commit` 실행 시 자동으로 빌드됩니다!

---

## 📋 체크리스트

코드를 푸시하기 전에 확인:

- [ ] 소스 코드 수정 완료
- [ ] `npm run build` 실행
- [ ] `dist/` 폴더 변경사항 확인
- [ ] 소스와 빌드 파일 모두 커밋
- [ ] 로컬에서 테스트 완료
- [ ] 푸시 전 최종 확인

---

## 🔍 문제 해결

### Q: 소스를 수정했는데 dist/가 업데이트되지 않았어요

**A:** 빌드를 실행하지 않았을 가능성이 높습니다.
```bash
npm run build
git add dist/
git commit --amend --no-edit
git push --force
```

### Q: PR을 만들었는데 CI가 실패합니다

**A:** dist/가 최신 상태인지 확인하세요.
```bash
npm run build
git diff dist/  # 변경사항 확인
```

### Q: 왜 dist/를 커밋에 포함해야 하나요?

**A:** Smithery 배포 플랫폼이 빌드된 파일을 요구하기 때문입니다. 대안:
- NPM에 발행하여 Smithery가 NPM에서 가져오도록 함
- Smithery의 빌드 시스템 개선 대기

---

## 🎯 모범 사례

1. **매 커밋마다 빌드**
   - 소스 수정 → 빌드 → 커밋

2. **PR 전 확인**
   - `npm run build` 실행
   - `git status`로 dist/ 변경 확인

3. **자동화 활용**
   - Pre-commit hook 설정
   - CI/CD에 빌드 검증 추가

---

## 📞 도움말

문제가 있으면:
- GitHub Issues: https://github.com/Won-ahamada/KERIS_EDUmcp/issues
- 빌드 문제: `npm run build` 로그 확인

---

**⚠️ 이 안내를 항상 기억하세요!**

소스 수정 → **빌드** → 커밋 → 푸시
